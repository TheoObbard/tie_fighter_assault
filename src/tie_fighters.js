class TieFighter{
  constructor(pos, vel) {
    let pos = pos
    let vel = vel
  }

  destroy() {
    //destroys them
  }

  fire() {
    //they shoot at us and we take damage
  }
}

module.exports = TieFighter